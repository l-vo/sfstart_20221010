{% extends 'base.html.twig' %}

{% block title %}Add movie{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center" id="movie-form">
        <div>
            <h1>Movie</h1>
            <div>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary">Submit</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
